---
- name: Test Loki deployment
  hosts: localhost
  become: true
  vars:
    loki_version: "3.5.1"
    loki_path: "/opt/loki"
    loki_http_port: "3100"
    loki_docker_network: "loki-net"
  roles:
    - loki
