global:
  resolve_timeout: 5m

route:
  receiver: 'telegram'
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
- name: 'telegram'
  telegram_configs:
  - bot_token: "{{ telegram_bot_token }}"
    chat_id: {{ telegram_chat_id | int }}
    send_resolved: true
